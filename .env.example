# ============================================
# 中国A股量化交易系统 - Docker Compose环境变量
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 不要将 .env 文件提交到版本控制系统
#

# -------------------- PostgreSQL配置 --------------------
# 数据库名称
POSTGRES_DB=quant_trading

# 数据库用户名
POSTGRES_USER=postgres

# 数据库密码（生产环境请使用强密码）
POSTGRES_PASSWORD=postgres

# 数据库端口（宿主机映射端口）
POSTGRES_PORT=5432

# -------------------- Redis配置 --------------------
# Redis端口（宿主机映射端口）
REDIS_PORT=6379

# Redis密码（生产环境建议设置）
REDIS_PASSWORD=

# -------------------- 后端服务配置 --------------------
# 后端服务端口（宿主机映射端口）
BACKEND_PORT=8000

# CORS允许的来源
CORS_ORIGINS=http://localhost

# API请求限流（每分钟最大请求数）
RATE_LIMIT_PER_MINUTE=60

# 日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）
LOG_LEVEL=INFO

# -------------------- 前端服务配置 --------------------
# 前端服务端口（宿主机映射端口）
FRONTEND_PORT=80

# API基础URL（前端访问后端的地址）
VITE_API_BASE_URL=http://localhost:8000

# -------------------- 生产环境额外配置 --------------------
# 以下配置仅在使用 docker-compose.prod.yml 时需要

# 应用域名
# DOMAIN=yourdomain.com

# SSL证书路径
# SSL_CERT_PATH=./nginx/ssl/cert.pem
# SSL_KEY_PATH=./nginx/ssl/key.pem

# 备份目录
# BACKUP_DIR=./backup

# -------------------- 开发环境配置示例 --------------------
# 开发环境使用默认值即可，无需修改

# -------------------- 生产环境配置示例 --------------------
# 生产环境配置示例（取消注释并修改）：
#
# POSTGRES_DB=quant_trading_prod
# POSTGRES_USER=quant_user
# POSTGRES_PASSWORD=your-strong-password-here
# POSTGRES_PORT=5432
#
# REDIS_PASSWORD=your-redis-password-here
# REDIS_PORT=6379
#
# BACKEND_PORT=8000
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# RATE_LIMIT_PER_MINUTE=100
# LOG_LEVEL=WARNING
#
# FRONTEND_PORT=80
# VITE_API_BASE_URL=https://api.yourdomain.com
#
# DOMAIN=yourdomain.com
